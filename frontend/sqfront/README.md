````
group by -> to create a specific group
the attribute used for groupby can only be used for select / aggregrate func like max min count 
1) display all the department name and count of employees working in that dept
   
Select dept,count(*)  from emp  group by dept 
````